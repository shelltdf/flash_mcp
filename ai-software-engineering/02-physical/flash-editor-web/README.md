# flash-editor-web

| 项 | 内容 |
|----|------|
| 产物 | 静态资源目录 `dist/`（由 `npm run build` 生成） |
| 源码根 | 仓库根下 `flash/`（相对实现子项目） |
| 构建 | Vite；包名 `flash` |
| 对应关系 | `npm run dev` → 开发服务器；`vite build` → `flash/dist/` |
